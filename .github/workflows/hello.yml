# workflow
#Should Have Name
name: hello

on:
  push:

#jobs
#Should Be Inside the Workflow
jobs: 
  say-hello: 
    runs-on: ubuntu-latest
    steps:
      - name: Say hello to everyone
        run: echo "Hello Dosto"
